services:
  - type: web
    name: tripcompass
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m tripcompass.api_server
    envVars:
      - key: PORT
        value: 10000
      - key: FLASK_DEBUG
        value: "False"
    healthCheckPath: /api/health

